{% extends 'base.html.twig' %}

{% block title %}Permission{% endblock %}

{% block body %}
    <h1>Permission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ permission.id }}</td>
            </tr>
            <tr>
                <th>Contexts</th>
                <td>{{ permission.contexts ? permission.contexts|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ permission.class }}</td>
            </tr>
            <tr>
                <th>Field</th>
                <td>{{ permission.field }}</td>
            </tr>
            <tr>
                <th>Operation</th>
                <td>{{ permission.operation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('permission_index') }}">back to list</a>

    <a href="{{ path('permission_edit', {'id': permission.id}) }}">edit</a>

    {{ include('permission/_delete_form.html.twig') }}
{% endblock %}
